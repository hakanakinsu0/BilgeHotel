@{
    ViewData["Title"] = "Bilgilendirme";
}

<h2 class="text-center">Hesap Aktivasyonu Gerekli</h2>

@if (TempData["Message"] != null)
{
    <div class="alert alert-info text-center">
        @TempData["Message"]
    </div>
}

<p class="text-center">
    <strong>Hesabınız oluşturuldu!</strong> Lütfen hesabınızı doğrulamak için e-postanızı kontrol edin ve size gönderilen aktivasyon linkine tıklayın.
</p>

<div class="text-center mt-4">
    <a href="@Url.Action("SignIn", "Home")" class="btn btn-primary">Giriş Yap</a>
</div>
